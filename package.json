{
  "name": "slush-sisters",
  "private": true,
  "version": "0.0.0",
  "description": "slushsisters.com — frozen drink rentals in Lakeway, TX",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
